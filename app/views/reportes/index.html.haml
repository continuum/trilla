= render(:partial => "shared/reportmenu")
.component-navigator-tabs
  %ul
    %li.tab-select.first
      %a#tab-form{:href => "javascript:void(0)"} Mis reportes
    %li.clearfix
.component-datatable
  -if @reportes.empty?
    No existen reportes creados
  -else
    .component-standar-grid
      %table.standar-grid-model{:border => "0", :cellpadding => "0", :cellspacing => "0"}
        %col{:width => ""}/
        %col{:width => ""}/
        %col{:width => "200px"}/
        %col{:width => "180px"}/
        %tbody
          %tr
            %th Nombre
            %th Descripci&oacute;n
            %th Query
            %th Tipo
            %th
          - @reportes.each do |reporte|
            %tr[reporte, :tr]
              %td= h reporte.nombre
              %td= h reporte.descripcion
              %td= h reporte.query
              %td= h reporte.tipo
              %td
                \&nbsp;
                \#{link_to 'Eliminar', reporte, :confirm => 'Está seguro?', :method => :delete}&nbsp;
  %br/
  = link_to 'Crear reporte', new_reporte_path