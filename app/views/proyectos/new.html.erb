<h1>New proyecto</h1>

<% form_for(@proyecto) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :descripcion %><br />
    <%= f.text_field :descripcion %>
  </p>
  <p>
	<%= f.label :cliente %><br />
	
	<% clientes_array = Cliente.all.map { |cli| [cli.descripcion, cli.id] } %>
	<%= select_tag(:cliente, options_for_select(clientes_array)) %> 
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', proyectos_path %>