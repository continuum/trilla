= render(:partial => "shared/adminmenu")
%h3 Listado de proyectos
.component-standar-grid
  %table.standar-grid-model{:border => "0", :cellpadding => "0", :cellspacing => "0"}
    %col{:width => ""}/
    %col{:width => ""}/
    %col{:width => "200px"}/
    %col{:width => "180px"}/
    %tbody
      %tr
        %th Descripcion
        %th Cliente
        %th Creado el
        %th
      - @proyectos.each do |proyecto|
        %tr
          %td= link_to proyecto.descripcion, proyecto
          %td= h proyecto.cliente.descripcion
          %td
            = h proyecto.created_at
            = h proyecto.archivado
          %td
            \&nbsp;
            \#{link_to 'Editar', edit_proyecto_path(proyecto)}&nbsp;
            \#{link_to 'Eliminar', proyecto, :confirm => 'Está seguro?', :method => :delete}&nbsp;
            \#{link_to 'Archivar', archive_proyecto_path(proyecto)}&nbsp;
%br/
= link_to 'Nuevo proyecto', new_proyecto_path
