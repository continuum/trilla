Setup en desarrollo
===================

$ gem install bundler
$ bundle install
$ cp config/database.yml.example config/database.yml

Editar config/database.yml.

$ rake db:create
$ rake db:migrate
$ script/server
